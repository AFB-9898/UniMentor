# OpenSpec Configuration — UniMentor
# Auto-generated by sdd-init. Do not edit the `testing:` block manually.

context: |
  UniMentor is an EdTech mentorship marketplace MVP (Software Engineering II).
  Built with Vite 6 + React 19 + TypeScript 5.8 + Tailwind CSS 4.
  Atomic Design architecture (atoms → molecules → organisms → screens).
  Backend via InsForge (BaaS, PostgreSQL, Auth, Storage).
  Bilingual documentation (EN/ES). Tests with Vitest + testing-library/jsdom.

strict_tdd: true

testing:
  detected: 2026-05-28
  runner:
    command: npm run test
    framework: Vitest 4.1.6
    watch: npm run test:watch
  layers:
    unit:
      available: true
      tool: Vitest
    integration:
      available: true
      tool: Vitest + @testing-library/react
    e2e:
      available: false
      tool: —
  coverage:
    available: false
    command: —
  quality:
    linter:
      available: false
      command: —
    type_checker:
      available: true
      command: tsc -b (via npm run build)
    formatter:
      available: false
      command: —

phases:
  propose: openspec/changes/{change-id}/proposal.md
  spec: openspec/changes/{change-id}/spec.md
  design: openspec/changes/{change-id}/design.md
  tasks: openspec/changes/{change-id}/tasks.md
  apply: openspec/changes/{change-id}/
  verify: openspec/changes/{change-id}/verify.md
  archive: openspec/changes/archive/
